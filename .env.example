# ============================================
# SD4 Environment Variables
# Copy to .env and fill in values
# ============================================

# Database (Supabase PostgreSQL)
# "Tenant or user not found" = wrong URL. Get the EXACT strings from:
#   Supabase Dashboard → Settings → Database → Connection string (URI)
#
# Required:
# - Username must be: postgres.[project-ref] OR prisma.[project-ref]
#   (project-ref is in your project URL, e.g. https://abcdefgh.supabase.co → abcdefgh)
# - Host must be the POOLER host for your region, e.g.:
#   aws-0-ap-southeast-2.pooler.supabase.com (or the one shown in the dashboard)
# - Port 6543 = transaction pooler (use this for the app)
# - Use the database password from Settings → Database (not your account password)
#
# Transaction pooler (port 6543) – use this for DATABASE_URL
DATABASE_URL="postgres://postgres.[PROJECT-REF]:[YOUR-DATABASE-PASSWORD]@[POOLER-HOST]:6543/postgres"
# Example (replace with your project ref, password, and region):
# DATABASE_URL="postgres://postgres.abcdefghij:YourPassword@aws-0-ap-southeast-2.pooler.supabase.com:6543/postgres"

# Direct/session connection (port 5432) – REQUIRED for Prisma migrations.
# Use the "Session" connection string from Supabase (same host, port 5432).
# Without this, "prisma migrate deploy" can hang or fail with "prepared statement already exists".
DIRECT_URL="postgres://postgres.[PROJECT-REF]:[YOUR-DATABASE-PASSWORD]@[POOLER-HOST]:5432/postgres"

# Demo access password (optional - when set, gates the app behind /demo-login)
# Leave unset for local dev to skip password gate
# DEMO_ACCESS_PASSWORD="your-secret-password"
